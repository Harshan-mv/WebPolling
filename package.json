{
  "name": "polling-backend",
  "version": "1.0.0",
  "description": "Backend for polling website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",   
  "client": "react-scripts start",  
  "build": "react-scripts build",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
